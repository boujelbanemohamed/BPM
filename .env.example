# Identifiants PostgreSQL utilisés par le conteneur "postgres" du
# docker-compose. DOIVENT correspondre exactement au DATABASE_URL défini
# dans backend/.env (même utilisateur, même mot de passe, même base).
POSTGRES_USER=bpmpro
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=bpm_pro
